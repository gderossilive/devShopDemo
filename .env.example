# devShop Azure deployment configuration template
# Copy this file to `.env` and populate values before running ./deploy.sh

# Azure region to deploy resources (e.g., westeurope, eastus)
AZURE_LOCATION=westeurope

# Your public IP address for RDP access restriction (leave blank to allow from anywhere)
MY_IP=

# Optional: specify Azure subscription (leave blank to use current default context)
AZURE_SUBSCRIPTION_ID=

# Optional: default azd environment name
AZURE_ENV_NAME=dev

# Optional: Azure Developer CLI log verbosity (trace|debug|info|warn|error)
AZURE_LOG_LEVEL=info

# SQL Server administrator credentials (required)
SQL_ADMIN_USERNAME=sqladmin
SQL_ADMIN_PASSWORD=ChangeMe123!

# Web VM administrator credentials (required)
WEB_ADMIN_USERNAME=webadmin
WEB_ADMIN_PASSWORD=ChangeMe456!
