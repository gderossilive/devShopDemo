# devShop Azure deployment configuration
# Fill in the required values before running ./deploy.sh
# IMPORTANT: Do not commit this file to source control with real secrets.

# Azure region used for all resources (e.g., westeurope, eastus)
AZURE_LOCATION=westeurope

# Your public IP address for RDP access restriction (leave blank to allow from anywhere)
MY_IP=81.56.0.189

# Optional: specify the Azure subscription to target (leave blank to use current default)
AZURE_SUBSCRIPTION_ID=06dbbc7b-2363-4dd4-9803-95d07f1a8d3e
TENANT_ID=86d068c0-1c9f-4b9e-939d-15146ccf2ad6

# Optional: default azd environment name (can be overridden via ./deploy.sh <name>)
AZURE_ENV_NAME=devShop

# Optional: adjust Azure Developer CLI log verbosity (trace, debug, info, warn, error)
AZURE_LOG_LEVEL=info

# SQL Server administrator credentials (required)
SQL_ADMIN_USERNAME=sqladmin
SQL_ADMIN_PASSWORD=ChangeMe123!

# Web VM administrator credentials (required)
WEB_ADMIN_USERNAME=webadmin
WEB_ADMIN_PASSWORD=ChangeMe456!
